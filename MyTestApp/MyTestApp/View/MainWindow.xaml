<Window
    x:Class="MyTestApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyTestApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="525"
    Height="350"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ListView
            Grid.Row="0"
            Grid.Column="0"
            Padding="20"
            Background="BlueViolet"
            ItemsSource="{Binding Path=MainVM.List}" />
        <Button
            Grid.Row="1"
            Grid.Column="0"
            Margin="50"
            Background="Aqua"
            Command="{Binding MainVM.OpenCommand}"
            Content="Открыть дочернее окно для создания нового элемента коллекции" />
    </Grid>
</Window>
